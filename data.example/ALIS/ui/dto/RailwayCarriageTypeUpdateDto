dto DTO.RailwayCarriageTypeUpdateDto {
  type Table;
  label "Тип железнодорожного вагона обновление";

  attribute id {
    type UUID;
    is required;
    key primary;
  }

  attribute code {
    type varchar(200);
    is required;
  }

  attribute active {
    type boolean;
    is required;
  }
}
