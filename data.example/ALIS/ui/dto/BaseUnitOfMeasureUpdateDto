dto DTO.BaseUnitOfMeasureUpdateDto {
  type Table;
  label "Базовый DTO обновления единицы измерения";

  attribute id {
    type UUID;
    is required;
    key primary;
  }

  attribute code {
    type varchar(200);
    is required;
  }

  attribute active {
    type boolean;
    is nullable;
  }

  attribute name {
    type varchar(500);
    is nullable;
  }

  attribute unitOfMeasureType {
    type varchar(50);
    is required;
  }
}
