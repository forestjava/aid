dto DTO.UnitOfMeasureCreationDto {
  type Table;
  label "DTO создания единицы измерения";

  attribute code {
    type varchar(200);
    is required;
  }

  attribute active {
    type boolean;
    is nullable;
  }

  attribute name {
    type varchar(500);
    is nullable;
  }
}
