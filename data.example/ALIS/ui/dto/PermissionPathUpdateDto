dto DTO.PermissionPathUpdateDto {
  type Table;
  label "DTO обновления пути разрешения";

  attribute id {
    type UUID;
    is required;
    key primary;
  }

  attribute path {
    type varchar(500);
    is nullable;
  }

  attribute type {
    type varchar(50);
    is nullable;
  }
}
