dto DTO.PermissionPathDto {
  type Table;
  label "DTO пути разрешения";

  attribute id {
    type UUID;
    is nullable;
    key primary;
  }

  attribute path {
    type varchar(500);
    is nullable;
  }

  attribute type {
    type varchar(50);
    is nullable;
  }
}
