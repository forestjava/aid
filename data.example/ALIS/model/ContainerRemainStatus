// Статус остатка контейнера
entity ALIS.ContainerRemainStatus {
  source "db-layer/src/main/java/ru/korusconsulting/alis/model/containerremainstatus/ContainerRemainStatus.java";

  attribute id {
    key primary;
    type String(30);
    is private;
    map alis.container_remain_status.id;
  }

  attribute code {
    type String(30);
    is private;
    is nullable;
    map alis.container_remain_status.code;
  }
}
