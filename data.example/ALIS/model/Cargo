// Контейнер (грузовое место)
entity ALIS.Cargo {

  attribute id {
    key primary;
    type UUID;
    is private;
    map alis.cargo.id;
  }

  attribute code {
    type String(100);
    is private;
    map alis.cargo.code;
  }
}
